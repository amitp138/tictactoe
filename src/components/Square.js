import React from 'react'

const Square = ({value}) => {
  return (
    <div className='square'>
        <button type='button' className='sqaure'>{value}</button>
    </div>
  )
}

export default Square